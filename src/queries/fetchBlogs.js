import { graphql, useStaticQuery } from "gatsby";

const FetchBlogs = (type) => {
  switch (type) {
    case "recipes":
      //
      break;

    case "blog":
      //
      break;

    default:
    //
  }
};
